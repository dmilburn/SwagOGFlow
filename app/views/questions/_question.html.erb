<h2><%= link_to question.title, question_path(question) %></h2>
<%= link_to question.user.name, user_path(question.user.id) %><br>
Body:
<%= question.body %><br>
Created at:
<%= question.created_at %><br>
Tags:
<% question.tags.each do |tag|%>
  <%= tag.name %>
<% end %><br>
<%= question.votes %><br>
<% if current_user %>
  <%= render '/votes/new' %>
<% end %>


