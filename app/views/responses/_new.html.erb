<%= form_for @response do |f| %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div><br>

  <div class="action">
    <%= f.submit "Add Response" %>
  </div>
<% end %>